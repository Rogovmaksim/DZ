time = int(input("Введите время в секундах "))
day = time // 86400
hours = (time // 3600) % 24
minutes = (time // 60) % 60
seconds = time % 60
print (f"Время в формате дд:чч:мм:сс {day} :  {hours} : {minutes} : {seconds}")
